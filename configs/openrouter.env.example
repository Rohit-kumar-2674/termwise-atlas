# Aider / compatible clients: load securely using the client documentation.
# A blank template does not automatically set your process environment.
OPENROUTER_API_KEY=
# Choose an exact current catalog ID; there is no permanently free model ID.
OPENROUTER_MODEL=PROVIDER/MODEL
